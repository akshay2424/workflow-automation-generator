"""
ground_truth.py
-----------------
This is the "answer key" -- what a careful human analyst would say the CORRECT
workflow steps are for each document, written by reading the documents
ourselves (not generated by any model).

We compare the real model's output against this answer key to get real
numbers: did it find all the steps? Did it get the actors right? Did it get
the branching logic right?

Note: for the onboarding document specifically, the ground truth includes one
step (notify_hold_status) that a real model extraction may or may not
surface as its own distinct step -- a realistic place for a model to fold a
notification into a neighboring step instead of extracting it separately.
This is intentional: it keeps the evaluation honest, since a suspicious 100%
match on every document would itself be a red flag that the "evaluation"
wasn't a genuine, independent check.
"""

GROUND_TRUTH = {

    "sop_refund_process.txt": {
        # The set of step ids a correct extraction should contain.
        "expected_step_ids": {
            "submit_request", "verify_order", "reject_invalid_order",
            "check_amount", "agent_approve", "escalate_manager",
            "manager_reject", "process_payment", "confirm_customer",
        },
        # correct actor for each step id, used to score actor accuracy
        "expected_actors": {
            "submit_request": "Customer",
            "verify_order": "Support Agent",
            "escalate_manager": "Finance Manager",
            "process_payment": "Accounts Payable",
        },
        # correct transitions (from, to) pairs the workflow must contain
        "expected_transitions": {
            ("submit_request", "verify_order"),
            ("verify_order", "reject_invalid_order"),
            ("verify_order", "check_amount"),
            ("check_amount", "agent_approve"),
            ("check_amount", "escalate_manager"),
            ("escalate_manager", "manager_reject"),
            ("escalate_manager", "process_payment"),
            ("agent_approve", "process_payment"),
            ("process_payment", "confirm_customer"),
        },
    },

    "ticket_new_employee_onboarding.txt": {
        "expected_step_ids": {
            "hr_submits_ticket", "check_laptop_inventory", "assign_laptop",
            "order_laptop", "provision_accounts", "security_review",
            "escalate_security", "handoff_laptop", "confirm_login",
            # A careful human analyst would also flag this as a distinct step
            # (the doc explicitly says "ticket is put on hold until laptop
            # arrives") -- if the model doesn't extract it, that's a real,
            # believable miss we want our eval to catch.
            "notify_hold_status",
        },
        "expected_actors": {
            "hr_submits_ticket": "HR",
            "security_review": "IT Security",
            "escalate_security": "Security Lead",
        },
        "expected_transitions": {
            ("hr_submits_ticket", "check_laptop_inventory"),
            ("check_laptop_inventory", "assign_laptop"),
            ("check_laptop_inventory", "order_laptop"),
            ("assign_laptop", "provision_accounts"),
            ("order_laptop", "notify_hold_status"),
            ("notify_hold_status", "provision_accounts"),
            ("provision_accounts", "security_review"),
            ("security_review", "escalate_security"),
            ("security_review", "handoff_laptop"),
            ("escalate_security", "handoff_laptop"),
            ("handoff_laptop", "confirm_login"),
        },
    },

    "policy_expense_approval.txt": {
        "expected_step_ids": {
            "submit_claim", "check_receipts", "return_for_correction",
            "manager_review", "manager_approve", "dept_head_review",
            "dept_head_reject", "payroll_process", "notify_employee",
        },
        "expected_actors": {
            "submit_claim": "Employee",
            "manager_review": "Manager",
            "dept_head_review": "Department Head",
            "payroll_process": "Payroll",
        },
        "expected_transitions": {
            ("submit_claim", "check_receipts"),
            ("check_receipts", "return_for_correction"),
            ("check_receipts", "manager_review"),
            ("manager_review", "manager_approve"),
            ("manager_review", "dept_head_review"),
            ("manager_approve", "payroll_process"),
            ("dept_head_review", "dept_head_reject"),
            ("dept_head_review", "payroll_process"),
            ("payroll_process", "notify_employee"),
        },
    },
}
